services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: manhld2402/frontend
    container_name: frontend
    platform: linux/amd64
    volumes:
      - ".:/app"
    ports:
      - "8080:80"
